import { IUser } from './user';

export interface IAuth {
    user: IUser,
    message: string,
    token: string
}

export interface IAuth {
    newUser: IUser,
    message: string,
    token: string
}